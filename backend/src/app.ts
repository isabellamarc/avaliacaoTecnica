import express from 'express';
// import orderController from './controllers/orderController';

const app = express();

app.use(express.json());

// 1
// app.post('/', controller);


export default app;
// get - select - listar - 3
// app.get('/' (req, res) => )

// post - insert - criar 1

// put - update - atualizar - 2

// delete - 4

// app.listen(3000);